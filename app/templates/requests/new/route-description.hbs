{{page-title "Nieuwe aanvraag"}}

<AuHeading @level={{3}}>
  New aanvragen - routebeschrijving
</AuHeading>

<Form::NewInstance
  @onCreate={{this.onSelectNewPerson}}
  @form={{this.model.form}}
  @onCancel={{this.closeModal}}
  @buildSourceTtl={{this.buildSourceTtl}}
  @onSubmit={{this.onSubmit}}
/>
{{outlet}}