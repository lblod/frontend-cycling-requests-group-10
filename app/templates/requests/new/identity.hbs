{{page-title "Nieuwe aanvraag"}}

<AuHeading @level={{3}}>
  New aanvragen - identiteit
</AuHeading>

<Form::NewInstance
  @onCreate={{this.onSubmit}}
  @form={{this.model.formIdentity}}
  @onCancel={{this.closeModal}}
  @buildSourceTtl={{this.buildSourceTtl}}
/>
{{outlet}}