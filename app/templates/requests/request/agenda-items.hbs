{{page-title "Agendapunten"}}
<div class="au-o-box au-o-flow au-o-flow--large">
  <AuContent>
    {{#each @model as |approval|}}
      <p>{{approval.name}}</p>
      <h4>{{approval.consideration.titel}}</h4>
      <p>{{approval.consideration.beschrijving}}</p>
      <p></p>
    {{/each}}
  </AuContent>
</div>